var items_categories = new Array(100);

class Items_Categories
{
    constructor(items_categories_id, item_id, category_id)
    {
        this.items_categories_id = parseInt(items_categories_id);
        this.item_id = parseInt(item_id);
        this.category_id = parseInt(category_id);
    }
}